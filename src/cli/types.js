/**
 * Объект-обработчик команды
 *
 * @typedef CommandHandler
 * @type {object}
 *
 * @property {function} run Запускает обработку команды
 * @property {string} name Имя команды
 * @property {string} help Текст с информацией по использованию команды
 */
